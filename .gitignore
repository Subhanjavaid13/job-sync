node_modules/
.env
*.log
PRD.md
# IMPLEMENTATION_PLAN.md
# NOTE: data/ is intentionally NOT ignored — data/jobs.db is the dedupe
# persistence layer that the GitHub Actions workflow commits back each run.
